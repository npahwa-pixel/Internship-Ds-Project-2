# UI + serving
gradio>=4.0.0

# Core ML stack
torch
transformers>=4.40
datasets>=2.19
accelerate>=0.30
peft>=0.10
safetensors>=0.4
numpy>=1.24

# Tokenizers + model-specific deps
tokenizers>=0.15
sentencepiece>=0.1.99     # needed for DeBERTa tokenizers
protobuf>=4.25            # required for some tokenizer conversions (from_slow=True)

# Metrics / eval
seqeval>=1.2
scikit-learn>=1.2
evaluate>=0.4

# Utilities
pyyaml>=6.0
requests>=2.31
tqdm>=4.66
rich>=13.7

# macOS LibreSSL warning fix (optional but recommended)
urllib3<2
